`RunArchive/` is the canonical repo-local home for returned RL training artifacts.

Folder shape:

```text
RunArchive/<scenario>/runs/<run_id>/
```

Typical contents:

```text
run_summary.json
model.onnx
eval-notes.md
comparison.md
```
